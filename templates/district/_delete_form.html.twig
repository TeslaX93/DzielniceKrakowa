<form method="post" action="{{ path('app_district_delete', {'id': district.id}) }}" onsubmit="return confirm('Czy na pewno chcesz usunąć tą dzielnicę?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ district.id) }}">
    <button class="btn">Usuń</button>
</form>
